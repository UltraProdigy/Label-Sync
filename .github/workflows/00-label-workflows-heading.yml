name: "00 - Label Workflows:"

on:
  workflow_call:

permissions: {}

jobs:
  display-only:
    if: ${{ false }}
    runs-on: ubuntu-latest

    steps:
      - name: Display-only placeholder
        run: echo "This workflow is a sidebar heading and must never run."
